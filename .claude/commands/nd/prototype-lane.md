---
description: Explore an open question with N parallel throwaway variants —
  ceremony stripped, code that never merges, and a promotion path that carries
  only the winning approach
argument-hint: <item-id>
---

This command is a generated shim: the procedure lives in the canonical workflow `nahel/workflows/prototype-lane.md`.
Read that workflow document in full and follow it exactly, with arguments: $ARGUMENTS
Do not edit this file — regenerate it with `nahel install --agent claude` (workflow: prototype-lane).
