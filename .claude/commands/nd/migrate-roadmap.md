---
description: Adopt a store's existing work into the roadmap layer — journal the
  selected set, then create the nodes it names
argument-hint: <product-node-slug>
---

This command is a generated shim: the procedure lives in the canonical workflow `nahel/workflows/migrate-roadmap.md`.
Read that workflow document in full and follow it exactly, with arguments: $ARGUMENTS
Do not edit this file — regenerate it with `nahel install --agent claude` (workflow: migrate-roadmap).
