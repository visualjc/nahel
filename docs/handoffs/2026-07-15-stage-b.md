# Handoff — Stage B kickoff (nahel-core)

For: a fresh agent session in `~/projects/personal/nahel`. Produced from the founding conversation (ccpm repo session, 2026-07-14/15). Everything durable is already in repo docs — read those; this file carries only the conversational residue that isn't.

## Read first (in this order)

1. [AGENTS.md](../../AGENTS.md) — read-first chain, house rules, run contract
2. [docs/roadmap.md](../roadmap.md) — **the product roadmap**: locked decisions + Phase 0 scope (your target)
3. [docs/bootstrap-plan.md](../bootstrap-plan.md) — Stage B specifics (you are here)
4. [docs/scaffolding.md](../scaffolding.md) — ccpm/yolo usage rules during bootstrap

## Conversation residue (not fully captured elsewhere)

- **Constitution is unblessed.** [PRODUCT.md](../../PRODUCT.md) was drafted by the founding agent, transcribed from Jim's decisions, but Jim has not yet reviewed it. By Nahel's own rules it is human-owned: get Jim's review/amendments **before** other Stage B work. This is the first gate.
- **Schema before code.** Phase 0's load-bearing artifact is state schema v1 (`WorkItem`, `JournalEvent`, `Observation`, hot-state, plus `pause`/`claim`/`handback` semantics). Run a short `domain-modeling` pass *with Jim* on the schema before implementation; [CONTEXT.md](../../CONTEXT.md) is the seed vocabulary and must stay in lockstep.
- **Phase 0 PRD scope** (for `/pm:prd-new nahel-core`): schema v1 + CLI commands `init`, `log`, `status`, `brief`, `validate` + shim generator for **Claude Code only** (prefix `/nd:`). Nothing else — no providers, no UI, no extra agents.
- **Import-friendly frontmatter.** All PRDs/epics authored via ccpm scaffolding must keep `name`, `status`, `depends_on` clean — `nahel import --from-ccpm` (Stage C) is a product feature that will consume them.
- **Memory caveat**: the founding session's Claude auto-memory is keyed to the ccpm directory and does NOT follow to nahel sessions. The repo docs are the whole inheritance; if something seems missing, it belongs in a repo doc, not in memory.
- **Exit test for Stage B**: `nahel brief` outputs a briefing that would correctly onboard a fresh agent to the Nahel project itself.

## Execution mode

Interactive with Jim: PRD grill + schema domain-modeling. Then AFK: `/pm:prd-parse` → `/pm:epic-decompose` → Jim sanity-check → `/loop /yolo-afk-dev --recycle` per epic. TDD (`bun test`) for all CLI code; ADR-0011 invariants in force; humans merge.

## Suggested skills

- `domain-modeling` — the schema pass (and CONTEXT.md upkeep)
- `tdd` — all CLI implementation
- `grilling` / `grill-with-docs` — if constitution amendments or PRD sharpening need it
- ccpm `/pm:*` commands + `yolo-afk-dev`, `yolo-pr-review` (installed in this repo's `.claude/`)
