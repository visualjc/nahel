---
name: Workflow format, install shims, acceptance proofs, self-init
status: open
created: 2026-07-16T16:55:20Z
updated: 2026-07-16T17:35:09Z
github: https://github.com/visualjc/nahel/issues/11
depends_on: [4, 7, 8, 9, 10]
parallel: false
conflicts_with: []
---

# Task: Workflow format, install shims, acceptance proofs, self-init

## Description

Close the epic (PRD F10 + success criteria): define the canonical workflow doc format, implement `nahel install --agent claude`, write the two cross-cutting acceptance tests (end-to-end journey, two-worktree merge), self-initialize this repo's Nahel state, and stage the Stage B exit test.

## Acceptance Criteria

- [ ] Canonical workflow format: `nahel/workflows/*.md` with frontmatter (`name`, `description`, `args`); documented in a spec doc + one example workflow file
- [ ] `nahel install --agent claude [--prefix nd]`: generates a 3-line shim per workflow under `.claude/commands/<prefix>/`; idempotent regeneration; unknown agent → clear error; agent targets structured as a lookup table (later agents additive)
- [ ] **E2E journey test** (PRD criterion 2): scripted `bun test` on a temp repo — `init` → `item new` → `run start` → `log` → `item update`/`run update --phase` → `run end` → `brief` renders all required sections reflecting the above
- [ ] **Two-worktree merge test** (PRD criterion 8): parallel mutations + logs in two real git worktrees; merge conflict-free; `validate` exit 0 on result
- [ ] **Self-init**: run `nahel init` (merging with existing PRODUCT.md/CONTEXT.md — never overwriting) + create initial work items reflecting current state; `validate` exits 0 on this repo (PRD criterion 4)
- [ ] Stage B exit staged: generated brief for this repo saved for Jim's 5-point rubric judgment (PRD criterion 1); fresh-agent session instructions documented

## Technical Details

- Files: `src/commands/install.ts`, `src/install/agents.ts` (target table), `nahel/workflows/` example, `docs/workflow-format.md`, tests in `tests/e2e/`
- Journey + merge tests exercise only the public CLI (child-process invocations), not internals — they are the thesis proof
- Self-init happens in a PR like any change; its journal shows zero hand-edit events (PRD criterion 5)

## Dependencies

- [ ] 4, 7, 8, 9, 10 (everything — final integration task)

## Effort Estimate

- Size: M
- Hours: 8
- Parallel: false

## Definition of Done

- [ ] Code implemented test-first; E2E suites green
- [ ] Verified by driving: `/nd:` shim loads its workflow in a real Claude Code session
- [ ] This repo self-initialized, validate clean, brief generated for rubric judgment
- [ ] Code reviewed (yolo-pr-review pass)
