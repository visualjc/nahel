---
description: Run a planning-partner session — place the altitude, grill the
  human, fire research, and hand off the moment they say enough to start
argument-hint: "[node-slug-or-id]"
---

This command is a generated shim: the procedure lives in the canonical workflow `nahel/workflows/plan.md`.
Read that workflow document in full and follow it exactly, with arguments: $ARGUMENTS
Do not edit this file — regenerate it with `nahel install --agent claude` (workflow: plan).
