---
description: Detect available agent CLIs and models, then write the
  responsibility routing map via the CLI
---

This command is a generated shim: the procedure lives in the canonical workflow `nahel/workflows/setup-routing.md`.
Read that workflow document in full and follow it exactly, with arguments: $ARGUMENTS
Do not edit this file — regenerate it with `nahel install --agent claude` (workflow: setup-routing).
