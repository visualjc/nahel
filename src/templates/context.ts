/**
 * CONTEXT.md template — the glossary skeleton `nahel init` emits (PRD F2).
 * CONTEXT.md is the project's ubiquitous language: terms defined there are
 * used exactly and consistently in code, workflows, docs, and conversation.
 */
export const CONTEXT_TEMPLATE = `# <Project> — Ubiquitous Language

Glossary of the domain model. Terms here are used exactly and consistently in code, workflows, docs, and conversation. Sharpen a term the moment it wobbles — a wobbling term is a design bug.

<Group terms under headings that fit the domain. For each term: a bold name and a definition precise enough that two agents using it independently cannot diverge.>

## <Area>

- **<Term>** — <definition precise enough to settle arguments>.
`;
