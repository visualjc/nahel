---
description: Review a PR to sign-off — two cross-vendor reviewers, findings
  validated against HEAD, red-first fixes capped at three rounds, then the merge
  decision
argument-hint: <item-id>
---

This command is a generated shim: the procedure lives in the canonical workflow `nahel/workflows/review-loop.md`.
Read that workflow document in full and follow it exactly, with arguments: $ARGUMENTS
Do not edit this file — regenerate it with `nahel install --agent claude` (workflow: review-loop).
